/// Demo accounts used by the mock auth flow while the backend is not
/// connected. Keeping these in one place prevents the credentials from
/// drifting between the login screen, mock data and documentation.
class DemoAccounts {
  DemoAccounts._();

  static const String customerEmail = 'brian@example.com';
  static const String fundiEmail = 'james@example.com';
  static const String password = 'fundilink';
}
